; Modules to help upgrade content/modules
; ---------------

api = 2
core = 7.x

projects[cck][subdir] = migrate

projects[coder][subdir] = migrate

projects[field_convert][subdir] = migrate
projects[field_convert][version] = "1.x-dev"
